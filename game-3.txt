;const headB=new THREE.Mesh(new THREE.SphereGeometry(2.1,28,20),new THREE.MeshStandardMaterial({color:0xb12173,emissive:0x4c082f,emissiveIntensity:.7,roughness:.48,metalness:.25}));headB.position.y=3.4;headB.castShadow=true;g.add(headB);for(let i=0;i<8;i++){const a=i/8*Math.PI*2;const tent=new THREE.Group();for(let j=0;j<6;j++){const s=new THREE.Mesh(new THREE.SphereGeometry(.38-j*.035,9,7),pinkMat);s.position.set(Math.cos(a)*j*.48,1.7-j*.24,Math.sin(a)*j*.48);tent.add(s)}g.add(tent)}for(const x of[-.65,.65]){const e=new THREE.Mesh(new THREE.SphereGeometry(.26,12,8),whiteGlow);e.position.set(x,3.65,1.85);g.add(e)}const hat=new THREE.Mesh(new THREE.CylinderGeometry(1.65,1.9,.75,16),darkMat);hat.position.y=5.2;g.add(hat);const sign=makeSign('床\n屋','#ffd166','#220431');sign.scale.setScalar(.35);sign.position.set(0,6.4,0);g.add(sign);g.position.set(0,0,-17);scene.add(g);enemies.push({mesh:g,type:'boss',hp:650,maxHp:650,speed:.68,damage:22,radius:2.3,score:5000,hitCd:0,phase:0,charge:1.2,boss:true,shoot:1});announce('TAKO-KATSU\nLE POULPE COIFFEUR',2500);tone(55,1.2,'sawtooth',.12)}

function fire(){const now=state.elapsed;if(now-lastShot<.115)return;lastShot=now;let target=input.aim.clone();if(!Number.isFinite(target.x)||input.touchFiring||input.keys.Space){const n=nearestEnemy();if(n)target.copy(n.mesh.position).setY(1.2);else target.copy(player.position).add(new THREE.Vector3(0,0,-10))}
  const origin=player.position.clone().add(new THREE.Vector3(0,3.75,0));const dir=target.sub(origin).normalize();const len=25;
  const beam=new THREE.Mesh(new THREE.CylinderGeometry(.055,.12,len,8,1,true),new THREE.MeshBasicMaterial({color:state.shine>80?0xffffff:0x5cf4ff,transparent:true,opacity:.9,blending:THREE.AdditiveBlending,depthWrite:false}));beam.position.copy(origin).addScaledVector(dir,len/2);beam.quaternion.setFromUnitVectors(new THREE.Vector3(0,1,0),dir);scene.add(beam);shots.push({mesh:beam,life:.09});
  let best=null,bestT=Infinity;for(const e of enemies){const to=e.mesh.position.clone().add(new THREE.Vector3(0,1,0)).sub(origin);const t=to.dot(dir);if(t<0||t>len)continue;const d=to.clone().addScaledVector(dir,-t).length();if(d<e.radius+.45&&t<bestT){best=e;bestT=t}}
  if(best)hitEnemy(best,10+(state.combo-1)*.55,dir);tone(510+Math.random()*80,.045,'square',.025);headLight.intensity=25;setTimeout(()=>{if(headLight)headLight.intensity=8},55)}
function hitEnemy(e,damage,dir){e.hp-=damage;e.mesh.position.addScaledVector(dir,.16);state.shine=clamp(state.shine+1.8,0,100);e.mesh.traverse(o=>{if(o.material&&o.material.emissive){const old=o.material.emissiveIntensity;o.material.emissiveIntensity=3;setTimeout(()=>o.material&&(o.material.emissiveIntensity=old),55)}});burst(e.mesh.position.clone().add(new THREE.Vector3(0,1,0)),0x5cf4ff,5,.08);if(e.hp<=0)killEnemy(e)}
function killEnemy(e){const idx=enemies.indexOf(e);if(idx<0)return;enemies.splice(idx,1);scene.remove(e.mesh);burst(e.mesh.position.clone().add(new THREE.Vector3(0,1.2,0)),e.boss?0xffd166:0xff4fb3,e.boss?70:18,e.boss?.28:.16);state.kills++;state.combo=clamp(state.combo+1,1,12);state.comboTimer=3.2;state.score+=Math.round(e.score*state.combo);state.shine=clamp(state.shine+(e.boss?100:8),0,100);tone(e.boss?90:180,.12,e.boss?'sawtooth':'triangle',e.boss?.16:.045);if(Math.random()<.16||state.kills%9===0)spawnPickup(e.mesh.position);if(e.boss){state.bossDefeated=true;announce('LE POUVOIR DU FRONT\nEST ABSOLU',2800);flashScreen();for(let i=0;i<12;i++)setTimeout(()=>burst(new THREE.Vector3(rand(-8,8),rand(1,7),rand(-8,4)),choose([0xff4fb3,0x5cf4ff,0xffd166]),30,.22),i*120)}}
function spawnPickup(pos){const g=new THREE.Group();const orb=new THREE.Mesh(new THREE.IcosahedronGeometry(.42,1),new THREE.MeshStandardMaterial({color:0xfff7d0,emissive:0xffd166,emissiveIntensity:2.2,metalness:.75,roughness:.08}));g.add(orb);const ring=new THREE.Mesh(new THREE.TorusGeometry(.7,.06,8,28),new THREE.MeshBasicMaterial({color:0x5cf4ff,blending:THREE.AdditiveBlending}));ring.rotation.x=Math.PI/2;g.add(ring);g.position.copy(pos).setY(.8);scene.add(g);pickups.push({mesh:g,life:12,phase:rand(0,6)})}
function triggerSuper(){if(!running||state.shine<100)return;state.shine=0;announce('MIROIR DU NÉANT',1400);flashScreen();tone(60,.7,'sawtooth',.15);cameraShake=1.2;const wave=new THREE.Mesh(new THREE.RingGeometry(.5,.8,64),new THREE.MeshBasicMaterial({color:0xffffff,transparent:true,opacity:.85,side:THREE.DoubleSide,blending:THREE.AdditiveBlending,depthWrite:false}));wave.rotation.x=-Math.PI/2;wave.position.copy(player.position).setY(.25);scene.add(wave);particles.push({mesh:wave,life:.8,max:.8,ring:true});for(const e of [...enemies]){const d=e.mesh.position.distanceTo(player.position);if(d<18)hitEnemy(e,e.boss?170:999,e.mesh.position.clone().sub(player.position).normalize())}}
function dash(){if(!running||state.dashCd>0)return;state.dashCd=2;state.invuln=.38;let v=getMoveVector();if(v.lengthSq()<.1){const n=nearestEnemy();v=n?n.mesh.position.clone().sub(player.position).setY(0).normalize():new THREE.Vector3(0,0,-1)}player.position.addScaledVector(v,4.2);clampPlayer();cameraShake=.35;burst(player.position.clone().add(new THREE.Vector3(0,1,0)),0x5cf4ff,22,.16);tone(180,.13,'sawtooth',.06)}
function nearestEnemy(){let best=null,d=Infinity;for(const e of enemies){const dd=e.mesh.position.distanceToSquared(player.position);if(dd<d){best=e;d=dd}}return best}

function update(dt){state.elapsed+=dt;state.dashCd=Math.max(0,state.dashCd-dt);state.invuln=Math.max(0,state.invuln-dt);state.comboTimer-=dt;if(state.comboTimer<=0)state.combo=1;
  const newWave=1+Math.floor(state.elapsed/22);if(newWave!==state.wave){state.wave=newWave;announce(`VAGUE ${state.wave} — ${choose(['PERRUQUES EN COLÈRE','TONDEUSES DU DESTIN','SALARIÉS À TOUPET','BRILLANCE CRITIQUE'])}`,1800)}if(state.wave>=4&&!state.bossSpawned)spawnBoss();
  const move=getMoveVector(),speed=input.keys.ShiftLeft||input.keys.ShiftRight?7.8:5.8;player.position.addScaledVector(move,speed*dt);clampPlayer();
  let aim=input.aim.clone().sub(player.position);aim.y=0;if((input.touchFiring||input.keys.Space)&&nearestEnemy())aim=nearestEnemy().mesh.position.clone().sub(player.position).setY(0);if(aim.lengthSq()>.1){const target=Math.atan2(aim.x,aim.z);player.rotation.y=lerpAngle(player.rotation.y,target,1-Math.exp(-12*dt))}
  const firing=input.mouseDown||input.keys.Space||input.touchFiring;if(firing)fire();
  spawnTimer-=dt;const cap=7+state.wave*2+(state.bossSpawned&&!state.bossDefeated?0:3);if(spawnTimer<=0&&enemies.filter(e=>!e.boss).length<cap){spawnEnemy();spawnTimer=Math.max(.28,1.35-state.wave*.1)*rand(.65,1.25)}
  updateEnemies(dt);updateProjectiles(dt);updateEffects(dt);updatePickups(dt);updateMusic(dt);updateHUD();
  aura.rotation.z+=dt*2.4;aura.material.opacity=.34+Math.sin(state.elapsed*6)*.14+state.shine/250;head.material.roughness=.27-state.shine*.0018;headLight.intensity=8+state.shine*.12;arenaRing.rotation.z+=dt*.08;sakura.rotation.y+=dt*.006;moon.position.y=24+Math.sin(state.elapsed*.08)*1.2;
}
function getMoveVector(){let x=(input.keys.KeyD||input.keys.ArrowRight?1:0)-(input.keys.KeyA||input.keys.KeyQ||input.keys.ArrowLeft?1:0),z=(input.keys.KeyS||input.keys.ArrowDown?1:0)-(input.keys.KeyW||input.keys.KeyZ||input.keys.ArrowUp?1:0);x+=input.touchMove.x;z-=input.touchMove.y;const v=new THREE.Vector3(x,0,z);if(v.lengthSq()>1)v.normalize();return v}
function clampPlayer(){const r=Math.hypot(player.position.x,player.position.z);if(r>18.2){player.position.x*=18.2/r;player.position.z*=18.2/r}}
function lerpAngle(a,b,t){let d=(b-a+Math.PI)%(Math.PI*2)-Math.PI;return a+d*t}

function updateEnemies(dt){for(const e of [...enemies]){e.phase+=dt;e.hitCd-=dt;e.charge-=dt;const pos=e.mesh.position,to=player.position.clone().sub(pos);to.y=0;const d=to.length();to.normalize();let sp=e.speed;if(e.type==='clipper'&&e.charge<0){sp*=3.2;if(e.charge<-.5)e.charge=rand(2.4,4)}if(e.boss){sp*=.65;pos.x+=Math.sin(state.elapsed*.7)*dt*1.4;e.shoot-=dt;if(e.shoot<=0){for(let k=-2;k<=2;k++)shootEnemy(e,k*.16);e