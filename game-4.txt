.shoot=Math.max(.7,1.65-state.wave*.06)}}pos.addScaledVector(to,sp*dt);e.mesh.rotation.y=Math.atan2(to.x,to.z);e.mesh.position.y=e.boss?Math.sin(e.phase*1.4)*.25:Math.sin(e.phase*4)*.12;e.mesh.rotation.z=e.type==='wig'?Math.sin(e.phase*5)*.1:e.mesh.rotation.z;if(d<e.radius+1&&e.hitCd<=0){damagePlayer(e.damage);e.hitCd=.8;pos.addScaledVector(to,-1.4)}}}
function shootEnemy(e,spread=0){const start=e.mesh.position.clone().add(new THREE.Vector3(0,e.boss?3.2:1,0)),dir=player.position.clone().add(new THREE.Vector3(0,1,0)).sub(start).normalize();dir.applyAxisAngle(new THREE.Vector3(0,1,0),spread);const m=new THREE.Mesh(new THREE.SphereGeometry(e.boss?.28:.18,10,8),new THREE.MeshBasicMaterial({color:e.boss?0xff4fb3:0xffd166,blending:THREE.AdditiveBlending}));m.position.copy(start);scene.add(m);enemyBullets.push({mesh:m,vel:dir.multiplyScalar(e.boss?7:5),life:5,damage:e.boss?12:7})}
function updateProjectiles(dt){for(let i=enemyBullets.length-1;i>=0;i--){const b=enemyBullets[i];b.life-=dt;b.mesh.position.addScaledVector(b.vel,dt);b.mesh.scale.setScalar(1+Math.sin(state.elapsed*15)*.14);if(b.mesh.position.distanceTo(player.position.clone().add(new THREE.Vector3(0,1,0)))<.75){damagePlayer(b.damage);b.life=0}if(b.life<=0){scene.remove(b.mesh);enemyBullets.splice(i,1)}}}
function damagePlayer(n){if(state.invuln>0)return;state.hp=clamp(state.hp-n,0,100);state.combo=1;state.invuln=.35;cameraShake=.65;flash.style.background='#ff174d';flash.style.opacity='.42';setTimeout(()=>flash.style.opacity='0',80);tone(95,.18,'square',.07);if(state.hp<=0)gameOver()}

function burst(pos,color,count=15,size=.15){for(let i=0;i<count;i++){const m=new THREE.Mesh(new THREE.TetrahedronGeometry(rand(size*.5,size*1.4),0),new THREE.MeshBasicMaterial({color,transparent:true,opacity:1,blending:THREE.AdditiveBlending,depthWrite:false}));m.position.copy(pos);scene.add(m);particles.push({mesh:m,life:rand(.35,.8),vel:new THREE.Vector3(rand(-1,1),rand(.2,1.4),rand(-1,1)).normalize().multiplyScalar(rand(2,8)),spin:new THREE.Vector3(rand(-8,8),rand(-8,8),rand(-8,8))})}}
function updateEffects(dt){for(let i=shots.length-1;i>=0;i--){const s=shots[i];s.life-=dt;s.mesh.material.opacity=s.life/.09;if(s.life<=0){scene.remove(s.mesh);shots.splice(i,1)}}for(let i=particles.length-1;i>=0;i--){const p=particles[i];p.life-=dt;if(p.ring){const t=1-p.life/p.max;p.mesh.scale.setScalar(1+t*24);p.mesh.material.opacity=(1-t)*.85}else{p.mesh.position.addScaledVector(p.vel,dt);p.vel.y-=7*dt;p.mesh.rotation.x+=p.spin.x*dt;p.mesh.rotation.y+=p.spin.y*dt;p.mesh.rotation.z+=p.spin.z*dt;p.mesh.material.opacity=clamp(p.life*2,0,1)}if(p.life<=0){scene.remove(p.mesh);particles.splice(i,1)}}}
function updatePickups(dt){for(let i=pickups.length-1;i>=0;i--){const p=pickups[i];p.life-=dt;p.phase+=dt;p.mesh.rotation.y+=dt*2.5;p.mesh.position.y=.8+Math.sin(p.phase*4)*.2;if(p.mesh.position.distanceTo(player.position)<1.35){state.shine=clamp(state.shine+35,0,100);state.hp=clamp(state.hp+8,0,100);announce('CIRE IMPÉRIALE +35',900);tone(880,.14,'sine',.055);p.life=0}if(p.life<=0){scene.remove(p.mesh);pickups.splice(i,1)}}}
function updateHUD(){scoreEl.textContent=Math.floor(state.score).toLocaleString('fr-FR');waveEl.textContent=state.wave;comboEl.textContent=`×${state.combo}`;hpFill.style.width=`${state.hp}%`;shineFill.style.width=`${state.shine}%`;hpText.textContent=Math.ceil(state.hp);shineText.textContent=Math.floor(state.shine);$('#superBtn').style.filter=state.shine>=100?'brightness(1.8) drop-shadow(0 0 12px #ffd166)':'none'}
function announce(text,duration=1200){announcement.innerHTML=text.replace('\n','<br>');announcement.classList.add('show');clearTimeout(messageTimer);messageTimer=setTimeout(()=>announcement.classList.remove('show'),duration)}
function flashScreen(){flash.style.background='white';flash.style.opacity='.85';setTimeout(()=>flash.style.opacity='0',110)}

function ensureAudio(){if(audioCtx)return;try{audioCtx=new (window.AudioContext||window.webkitAudioContext)();master=audioCtx.createGain();master.gain.value=.65;master.connect(audioCtx.destination)}catch(e){}}
function tone(freq,dur=.1,type='sine',vol=.03,delay=0){if(!audioCtx||!master)return;const o=audioCtx.createOscillator(),g=audioCtx.createGain();o.type=type;o.frequency.setValueAtTime(freq,audioCtx.currentTime+delay);o.frequency.exponentialRampToValueAtTime(Math.max(30,freq*.72),audioCtx.currentTime+delay+dur);g.gain.setValueAtTime(.0001,audioCtx.currentTime+delay);g.gain.exponentialRampToValueAtTime(vol,audioCtx.currentTime+delay+.012);g.gain.exponentialRampToValueAtTime(.0001,audioCtx.currentTime+delay+dur);o.connect(g);g.connect(master);o.start(audioCtx.currentTime+delay);o.stop(audioCtx.currentTime+delay+dur+.03)}
function updateMusic(dt){if(!audioCtx)return;musicClock-=dt;if(musicClock<=0){musicClock=.26;const scale=[110,147,165,220,247,330,294,220];const f=scale[musicStep++%scale.length];tone(f,.18,musicStep%4===0?'sawtooth':'triangle',.012);if(musicStep%4===0)tone(55,.1,'square',.018)}}

function animate(){requestAnimationFrame(animate);const dt=Math.min(clock.getDelta(),.035);if(running&&!paused)update(dt);const base=new THREE.Vector3(0,18,23);if(cameraShake>0){cameraShake=Math.max(0,cameraShake-dt*1.9);base.x+=rand(-1,1)*cameraShake;base.y+=rand(-1,1)*cameraShake*.45;base.z+=rand(-1,1)*cameraShake}camera.position.lerp(base,1-Math.exp(-8*dt));camera.lookAt(player&&player.visible?player.position.clone().multiplyScalar(.13).add(new THREE.Vector3(0,1.2,0)):new THREE.Vector3(0,1,0));renderer.render(scene,camera);window.__DEBUG_STATE__={running,score:state.score,hp:state.hp,enemies:enemies.length,wave:state.wave}}

init();
